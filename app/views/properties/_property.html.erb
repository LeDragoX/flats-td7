<div class="property-table">
    <tr>
        <td><%= link_to(property.title, property_path(property.id)) %></td>
        <td><%= property.description %></td>
        <td>Quartos: <%= property.rooms %></td>
    </tr>
</div>